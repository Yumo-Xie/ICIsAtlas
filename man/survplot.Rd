% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survplot.R
\name{survplot}
\alias{survplot}
\title{Plot Kaplan–Meier survival curves}
\usage{
survplot(
  survival = c("OS", "PFS"),
  type = "FavTME",
  cut = c("median", "best"),
  by.study = T,
  available = F
)
}
\arguments{
\item{survival}{character; outcome, either \code{"OS"} or \code{"PFS"}.}

\item{type}{character; stratification variable (e.g.\ \code{"FavTME"} or cell-state name).}

\item{cut}{character; cutpoint method, \code{"median"} or \code{"best"}.}

\item{by.study}{logical; if \code{TRUE}, plots curves separately per study; else pooled.}

\item{available}{logical; if \code{TRUE}, prints available input types and exits invisibly.}
}
\value{
Returns plots.
}
\description{
KM curves (and risk tables) stratified by a chosen metric,
either per-study or pooled across all samples.
}
\examples{
\dontrun{
# Using available = T to see the available input types -----------------

survplot(available = T)

# Plot survival curves ----------------------
survplot(
  survival = "PFS",
  type     = "FavTME",
  cut      = "median",
  by.study = T
)
}
}
